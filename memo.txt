今の SaveButton の問題点（責めじゃなく整理ね）
❌ 問題①：usePluginForm を SaveButton で呼んでいる
const { methods, handleReset } = usePluginForm();


これはかなり危険 ⚠️
理由は👇

useForm が 別インスタンス になる可能性

FormProvider と不整合

isDirty / reset が壊れる

👉 useForm は1画面1回
👉 呼ぶのは PluginContent だけ。

❌ 問題②：保存境界が2つある
const onSave = handleSubmit(async (data) => {});


と

const onSaveButtonClick = useAtomCallback(...)


RHFの submit

jotaiの保存

が 分断されてる。

👉 これ一番事故りやすい。

❌ 問題③：SaveButton が「司令塔」になってる

jotai操作

loading制御

snackbar表示

history.back

これ全部👇
画面全体の責務。

SaveButton にあると
「再利用できない」「テストできない」になる。

正しい役割分担（再確認）
SaveButton の責務

isDirty / isValid / isSubmitting を見る

submit を発火する

PluginContent の責務
usePluginForm
保存処理
jotai更新
snackbar
reset
